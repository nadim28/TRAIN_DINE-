<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.marty.yummy">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TrainDine"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.LoginActivity"
            android:theme="@style/AppTheme.Launcher"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"
                    android:host="marty.yummy.com" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.IndividualActivity"
            android:theme="@style/AppTheme"
            android:parentActivityName=".ui.IndividualActivity"/>
        <activity android:name=".ui.CartActivity"
            android:parentActivityName=".ui.CartActivity"/>
        <activity android:name=".ui.TrainInfo"
            android:parentActivityName=".ui.TrainInfo"/>

        <activity android:name=".ui.HomeScreenActivity"
            android:parentActivityName=".ui.HomeScreenActivity"/>


    </application>

</manifest>